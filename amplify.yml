version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd my-portfolio
        - npm install
    build:
      commands:
        - cd my-portfolio
        - npm run build
  artifacts:
    baseDirectory: my-portfolio/.next
    files:
      - '**/*'
  cache:
    paths:
      - my-portfolio/node_modules/**/*
