version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd my-portfolio
        - npm install
    build:
      commands:
        - npm run build    # ✅ DO NOT cd again here!
  artifacts:
    baseDirectory: my-portfolio/.next
    files:
      - '**/*'
  cache:
    paths:
      - my-portfolio/node_modules/**/*
